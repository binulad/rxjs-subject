import { TextTruncateDirective } from './text-truncate.directive';

describe('TextTruncateDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTruncateDirective();
    expect(directive).toBeTruthy();
  });
});
